//each excercise must have interval of allowed index numbers of nodes and relations
module.exports=
    {
        defaultExcerciseTextAreaContent:'START root=node(0) // Start with the reference node\n'+
        'RETURN root        // and return it.'
    }
;